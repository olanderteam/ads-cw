# Vite Environment Variables (for frontend)
VITE_APIFY_API_TOKEN=your_apify_token_here

# Vercel Serverless Function Environment Variables (for backend proxy)
# This API key is used by the /api/ads proxy endpoint to authenticate with Scraper Creators API
# Set this in Vercel dashboard: Project Settings > Environment Variables
SCRAPER_CREATORS_API_KEY=your_scraper_creators_api_key_here

# Local Development Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual API keys
# 3. The frontend can optionally pass apiKey as a query parameter to override the server-side key
# 4. For production, set SCRAPER_CREATORS_API_KEY in Vercel dashboard
